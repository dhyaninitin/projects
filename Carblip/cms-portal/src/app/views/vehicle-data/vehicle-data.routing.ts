import { Routes } from '@angular/router';
import { VehicleDataComponent } from './vehicle-data.component';

export const VehicleDataRoutes: Routes = [
  {
    path: '',
    component: VehicleDataComponent,
  }
];
